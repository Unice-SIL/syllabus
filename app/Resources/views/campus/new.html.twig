{% extends 'admin_layout.html.twig' %}
{% set h1 = "admin.button.add_campus" | trans %}
{% set href = path('app.admin.campus.index') %}

{% block subContent %}

    <div class="card card-primary">
        <!-- /.card-header -->
        <!-- form start -->
        {{ form_start(form) }}
        <div class="card-body">
            {{ form_row(form.label) }}
            <div class="custom-control custom-switch">
                {{ form_widget(form.synchronized) }}
                {{ form_label(form.synchronized) }}
            </div>
        </div>
        <!-- /.card-body -->

        <div class="card-footer">
            <button type="submit" class="btn btn-primary">{{ 'admin.button.save' | trans }}</button>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}