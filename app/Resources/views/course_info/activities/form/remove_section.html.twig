{{ form_start(form, {'attr': {'class': 'modal-action-form', 'data-action-url': path('course_activities_remove_section', {'id': courseInfo.id, 'sectionId': form.vars.data.id})}}) }}
{{ form_errors(form) }}
<h5>Êtes-vous sûr(e) de vouloir supprimer la section "{{ form.vars.data.title}}" ?</h5>
<div class="d-flex justify-content-center">
    <button type="submit" class="btn btn-danger rounded mr-2">Supprimer</button>
</div>
{{ form_widget(form._token) }}
{{ form_end(form) }}