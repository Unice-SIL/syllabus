{{ form_start(form, {'attr': {'class': 'modal-action-form', 'data-action-url': path('course_equipment_equipment_remove', {'idCourseInfo': courseInfo.id, 'idResourceEquipment': form.vars.data.id}), 'data-card': 'card-equipment' }}) }}
{{ form_errors(form) }}
<h5>Êtes-vous sûr(e) de vouloir supprimer le matériel "{{ form.vars.data.equipment.label }}" ?</h5>
<div class="d-flex justify-content-center">
    <button type="submit" class="btn btn-danger rounded mr-2">Supprimer</button>
</div>
{{ form_widget(form._token) }}
{{ form_end(form) }}