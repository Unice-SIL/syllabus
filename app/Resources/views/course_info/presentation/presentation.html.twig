{% extends 'course_info/layout.html.twig' %}

{% block subContent %}

    <div class="card toggle-focus my-2 shadow card-syllabus"
         data-view-url="{{ path('course_presentation_general_view', {'id': courseInfo.id }) }}"
         data-form-url="{{ path('course_presentation_general_form', {'id': courseInfo.id }) }}"
    >
        <div class="card-header">
            <div class="d-flex justify-content-between">
                <h3>Général</h3>
                <button type="button" class="edit-button btn btn-primary button-gradient">
                    Mettre à jour
                </button>
            </div>
        </div>
        <div class="card-body">
        </div>
    </div>
    {#    <section>
            {% include('course_info/presentation/view/teachers.html.twig') %}
        </section>
        <section>
            {% include('course_info/presentation/view/teaching_mode.html.twig') %}
        </section>#}

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        $(document).ready(function() {

            $('#menu-presentation').addClass('active');
            $('#menu-presentation').addClass('border-bottom-3 border-white');

        });
    </script>
{% endblock %}