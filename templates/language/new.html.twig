{% extends 'admin_layout.html.twig' %}
{% set h1 = 'admin.button.add_language' | trans %}
{% set href = path('app.admin.language.index') %}

{% block subContent %}

    <div class="card card-primary">
        <!-- /.card-header -->
        <!-- form start -->
        {{ form_start(form) }}
        <div class="card-body">
            {{ form_row(form.label) }}
        </div>
        <!-- /.card-body -->

        <div class="card-footer">
            <button type="submit" class="btn btn-primary">{{ 'admin.button.save' | trans }}</button>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}